export type DailyRecord = {
  date: string;
  editor: string;
  amWork: string;
  pmWork: string;
  // onTime: boolean;
  // timeAdjustment: number;
};

export type Record = {
  situation: string;
  support: string;
  good: boolean;
  notice: boolean;
}

